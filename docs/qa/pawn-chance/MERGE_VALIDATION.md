# 发布前合并检查

用户授权推送后，核实线上main已前进至 `958c7def34a32baa3cb2936f57d163815e1948fe`，包含PR #20的七夜整合与周转提示，以及设计文档更新。本地先合入该提交，再完成补充检查。

## 合并结果

- 默认新游戏保留v20 `pawn_chance_seven`；保留线上新增的 `complete_seven` v19内容、自动位置与手动档兼容。
- v20同时显示库存周转概况和卖货预览，沿用线上整理后的账本布局。只扩展提示适用入口，不改变现金、息费、售价或当票结算。
- 线上原有回归组完整保留；补入v20概率、跨进程、提前赎当和真实界面检查。历史界面测试显式使用其原内容，避免新默认入口改变旧版验收含义。
- GDD V0.9本地副本与线上blob一致；同名副本先存入项目隔离目录，线上设计文档更改完整保留。

## 补充验证

15组全部通过，原始结果见 [merge-results.json](merge-results.json)，对应日志为同目录 `merge-*.txt`。

- v20概率核心41,813项，56个实际检查点；跨进程写入／篡改98项，独立读取10项。
- 周转计算、卖货预览、新旧入口恢复41项；旧整合版完整章节22,189项。
- v20市场与铜镜六路线7,410项；姜素云4,736项，独立进程567项。
- 1280×720、1600×900的v20活当界面各131项、卖货各427项、右下角来信各75项。
- 旧整合版真实界面465项；新增与原Windows回归脚本语法检查通过。

这是合入最新main后的补充检查；首次实现的22组记录仍保留在 [VALIDATION.md](VALIDATION.md)。未制作新的试玩包。
