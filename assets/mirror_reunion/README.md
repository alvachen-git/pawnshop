# 铜镜重逢演出素材

使用内置 ImageGen 生成，2026-09-17。原始PNG未裁切、重绘或重新编码；生成来源与每张提示词保存在 `provenance/prompts.json`。丈夫常态直接引用 `../art04/customers/ordinary/hawker.png`，柜台、查访会面与对质共用 `CounterVisualCatalog` 身份入口。

| 人物 | 状态文件 |
| --- | --- |
| 丈夫 | 常态：现有货郎；`husband_surprise` 惊讶、`husband_evasive` 回避、`husband_remorse` 歉意、`husband_angry` 愤怒、`husband_terrified` 惊恐 |
| 女子 | `wife_waiting` 等待、`wife_questioning` 质问、`wife_sorrow` 悲伤、`wife_relief` 释然、`wife_disappointed` 失望、`wife_reaching` 怨怒伸手 |

上述立绘在 `portraits/`，采用相同方形画布及腰部裁切位置，渲染时保持比例。源图透明通道保留，既有色键着色器同时清理参考图可能残留的洋红细边。

| 插画 | 文件 |
| --- | --- |
| 迟来的道歉 | `endings/acknowledged.png` |
| 放下旧怨 | `endings/released.png` |
| 以怨相留 | `endings/resentment.png` |
| 失望离去 | `endings/disappointed.png` |

宽幅插画只覆盖上方演出区，等比显示。对白、操作和结果通知由游戏绘制，不在图内。无儿童现身、彼岸重逢或血腥遗体画面。

实际游戏截图见 `../../docs/qa/mirror-performance/`；美术浏览页见 [gallery.html](gallery.html)。
